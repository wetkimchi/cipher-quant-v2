import { extractAddresses, extractBuySizes } from "./common/nansen";

const channelId = "1254984824989548554";
const displayName = "TOP_DEGEN_WALLETS";

export const TopDegenWalletsChannel = {
  channelId,
  displayName,
  extractAddresses,
  extractBuySizes,
};
